import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        int[] numbers = {3, 7, 1, 9, 4};
        Arrays.sort(numbers);

        for (int num : numbers) {
            System.out.print(num + " ");
        }
    }
}
